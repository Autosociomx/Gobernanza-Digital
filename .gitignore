node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Firebase config contiene apiKey real — NO commitear (hallazgo E3 Acta 006)
firebase-applet-config.json

# Carpeta de trabajo temporal de auditoría
.openclaw/

# Recibos de arquitectura generados en cada corrida (el permanente se versiona
# en docs/marco/recibos/)
artifacts/recibo-arquitectura.yml
artifacts/recibo-arquitectura.json
